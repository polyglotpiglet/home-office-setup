---
- hosts: dev
  vars:
    - home_dir: /Users/alexbate/
    - applications: # these will be installed with brew cask idempotently
       - iterm2
       - intellij-idea-ce
       - java
       - haskell-platform
       - spectacle
       - telegram

  tasks:
    - name: Enable installing specific versions of casks (eg for intellij-idea-ce rather than intellij-idea)
      shell: brew tap caskroom/versions

    - name: Check for installed apps(casks)
      shell: brew cask list | grep {{ item }}
      register: installed_applications
      with_items: "{{ applications }}"
      ignore_errors: true

    - name: Install apps with brew-cask   
      shell: brew cask install {{ item }}
      with_items: "{{ applications }}"
      when: item not in installed_applications.results|map(attribute='stdout')

    - name: install brew packages
      homebrew: update_homebrew=yes name={{ item }}
      with_items:
        - npm
        - nvm
        - vcsh
        - mr
        - maven
        - htop
        - wget
        - scala
        - sbt
        - tig
        - python
        - tmux
        - zsh
        - zsh-completions
        - tree
        - watch
        - couchdb
        - postgresql
      tags:
        - brew

      ######################### vundle ####################################

    - name: check if vundle repo has been cloned (this step isnt needed but i'm keeping it here for reference)
      stat: 
        path: ~/.vim/bundle/Vundle.vim
      register: vundle_exists 

    - name: checkout vundle
      git: 
        repo: git@github.com:VundleVim/Vundle.vim.git
        dest: ~/.vim/bundle/Vundle.vim

      ######################### mr ####################################

    - name: check if mr repo has been cloned
      command: cd ~
      command: vcsh mr rev-parse --verify master
      register: master
      failed_when: master.rc == 2

    - name: print response
      debug:
        msg: "Response code from check to see if mr exists in vcsh: {{ master.rc }} "

    - name: Clone mr with vcsh from polyglotpiglet
      command: vcsh clone 'git@github-personal:polyglotpiglet/mr.git' 'mr'
      when: master.rc == 1 

    - name: mr update
      command: chdir=~ mr update

      ######################### scm_breeze ####################################
    #- name: install scm_breeze
      #command: git clone git://github.com/scmbreeze/scm_breeze.git ~/.scm_breeze
      #command: ~/.scm_breeze/install.sh
      #command: source ~/.bashrc
